# 開始使用

## 只有一個步驟

**雙擊 `開啟建材彙整.cmd`。**

瀏覽器會自動開到 http://127.0.0.1:8000，就可以用了。要關掉就雙擊 `停止建材彙整.cmd`。

**不用安裝任何東西** —— 不用裝 Python，不用裝 SketchUp，不用開命令列，不需要系統管理員權限，
也不需要網路。第一次啟動大約 10 秒，之後更快。

> 不用執行 `setup.cmd`。那是給「想改用自己電腦上 Python」的人用的，一般情況不需要。

## 為什麼不用安裝

這個資料夾自己帶了跑起來需要的全部東西：

| 資料夾 | 是什麼 |
|---|---|
| `runtime\` | Python 3.12（官方 embeddable 版）＋ 所有套件。與系統上的 Python 完全無關，不會互相影響 |
| `vendor\sketchup-sdk\` | `SketchUpAPI.dll` —— 讀 `.skp` 用的，**不必安裝 SketchUp**，這個檔案就夠了 |
| `web\dist\` | 已建置好的前端，所以不需要 Node.js |

唯一的前提是 **Windows**：`.skp` 是未公開格式，唯一能可靠讀它的是 SketchUp 官方的
`SketchUpAPI.dll`，而官方沒有出 Linux 版。

## 試一下

首頁上傳 **`samples\Modern_House_Reference_02.skp`**（一棟兩層樓現代住宅，
13 種材質、890 個面）。轉檔幾秒鐘，完成後：

- **左鍵點任一面** → 右側顯示材質、長、寬、實際面積
- **中鍵拖曳**旋轉、**滾輪**縮放、**Shift + Z** 顯示全部
- 右上角 **「水平剖面」** 打開才點得到室內的地板與天花（不然被屋頂擋住）
- 下方彙整表可切「扣除重疊」、取消勾選非建材（草地、襯底量體）
- 可匯出 **PDF** 與 CSV

`samples\README.md` 列了每個數字的正確答案，可以拿來核對。

## 交給你的 Claude

在這個資料夾裡開 Claude Code：

```bash
claude
```

根目錄的 **`CLAUDE.md`** 會自動被讀進去，裡面寫了架構、每個模組負責什麼、
以及改動後必須做的事（例如**改了 `web/src/` 一定要 `npm run build`**，
因為伺服器只讀 `web/dist`）。

深入一點的原理 —— 面怎麼合併、長寬怎麼量、重疊面怎麼扣 —— 都在 `README.md`，
那是這個專案最完整的一份文件，值得先讀一遍。

## 出問題時

| 症狀 | 看這裡 |
|---|---|
| 網站打不開 | `logs\server.err.log`。DLL 是在 import 時載入的，載不到伺服器整個起不來 |
| 說找不到 `SketchUpAPI.dll` | `vendor\sketchup-sdk\` 裡應該要有 `SketchUpAPI.dll` 與 `SketchUpCommonPreferences.dll` 兩個檔（少一個就載不起來）。整包複製時漏掉了就補回去 |
| 上傳後說版本太新 | `.skp` 比 DLL 新。在 SketchUp 裡另存成舊版本，或換一份新版 SDK 的 DLL 放進 `vendor\sketchup-sdk\` |
| PDF 中文變空白 | 找不到系統中文字型。`server\report.py` 會依序找微軟正黑體／雅黑／細明體／新宋體／標楷體 |
| 改了程式沒反應 | 改 `converter/` 要重啟伺服器；改 `web/src/` 要 `cd web && npm run build` |
| 想改用自己的 Python | 把 `runtime\` 改名或刪掉，再執行 `setup.cmd` 建 `.venv`。啟動時 `runtime\` 優先，沒有才用 `.venv` |
